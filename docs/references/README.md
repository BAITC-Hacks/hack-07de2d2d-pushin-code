# Справочники — брать по желанию

Реализация свободна: стек, библиотеки, MCP, скилы — на выбор исполнителя. Здесь — готовые куски, которые экономят время, если подходят.

| Файл | Что |
|---|---|
| `trace-event-schema.md` | **единственное обязательное** — формат событий следа решения, его ждёт фронт и проверяет мастер |
| `agent_loop.py` | цикл агента на OpenAI tool calling + SSE, FastAPI, ~80 строк |
| `TracePanel.jsx` | панель следа решения, React, без зависимостей |
| `data-quickstart.md` | синтетика за 10 минут / профиль чужого датасета за 10 минут |
