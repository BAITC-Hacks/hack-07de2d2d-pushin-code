# Рубрика · **v2** · 22.09.2026 · положение в редакции 17.09.2026, п. 7.4 и 9.4

> v1 строилась на старой редакции критериев (функциональность 25 / потенциал 20 / README 10).
> Организатор переписал п. 7.4 целиком — v1 недействительна.

## Правила изменения
- Меняет только Абылай; мастер предлагает в отчёте цикла.
- **Ослабить якорь — только со ссылкой** на текст положения или объявление организатора. Ужесточить — свободно.
- Якорь допустим, только если **проверяем командой или скриншотом**. «Хорошая архитектура» — не якорь.
- Каждое изменение — новая версия в шапке; `SCORE.md` указывает версию.
- Утром 23.09: появилась **методика оценки** (п. 7.1) или у кейса свои обязательные условия
  допуска → v3 целиком под них. Нет → якоря специализируются под кейс, веса не трогаются.

## Этап 1 — технический отбор (по нему живём до freeze)

### Техническая реализация — 25
Дословно: «заявленная функциональность действительно реализована в коде, компоненты связаны,
структура понятна, ключевые функции **не заменены заранее подготовленными ответами или имитацией**».

| | Якорь | Проверка |
|---|---|---|
| 25 | на главном пути **нет ни одного мока**: агент сам выбирает инструмент, инструменты читают реальные данные из БД, последний вызов — действие с последствием; фронт ↔ API ↔ агент ↔ данные связаны; схема в README | `grep -rn "TODO\|mock\|fixture\|hardcod" backend/` по главному пути пуст; трейс показывает вызовы инструментов с разными аргументами на разных входах |
| 18 | агент с инструментами работает, но один участок подменён фикстурой и это **честно помечено в README** | помеченный участок |
| 12 | инструменты объявлены, но модель их не выбирает — вызовы жёстко в коде по порядку | код |
| 6 | обёртка над чатом: один вызов модели, ответ на экран | |
| 0 | ключевая функция — заранее заготовленный ответ | |

### Соответствие кейсу и функциональность — 20
Дословно: «выполнены обязательные требования выбранного кейса; основной сценарий работает
полностью: от входных данных до результата; оценивается только фактически работающая функциональность».

| | Якорь | Проверка |
|---|---|---|
| 20 | **все** обязательные требования кейса выписаны списком в README и закрыты; сквозной сценарий проходит от ввода до результата без ручных шагов | прогон с чужого устройства, чек-лист требований в README |
| 14 | сценарий проходит целиком, одно необязательное требование кейса не закрыто | |
| 8 | сценарий проходит с ручным шагом посередине | |
| 4 | работают отдельные куски, сквозного прохода нет | |
| 0 | не проходит | |

*Если кейсы не опубликованы и задача = трек: «обязательные требования» = требования, которые
мы сами выписали в `docs/CONTRACT.md` как сквозной сценарий. Список в README обязателен всё равно.*

### README и техническая документация — 20
Дословно: README заменяет презентацию — что реализовано, как устроено, какие технологии и
данные, как установить и запустить, **как проверить основной сценарий**, какие ограничения
известны. «Оценивается содержание, а не визуальное оформление».

| | Якорь | Проверка |
|---|---|---|
| 20 | все семь блоков шаблона на месте, включая **«Как проверить основной сценарий»** (пошагово, с ожидаемым результатом) и **«Известные ограничения»**; раскрытие по п. 6.4; системные требования, зависимости и переменные окружения перечислены (п. 8.8) | человек, не писавший README, повторяет проверку сценария по нему и получает описанный результат; без этого подтверждения — потолок 12 |
| 12 | блоки есть, проверка сценария описана общо | |
| 6 | есть запуск, нет проверки сценария или ограничений | |
| 0 | заглушка от платформы | |

### Воспроизводимость и готовность к развёртыванию — 20
Дословно: «жюри может самостоятельно запустить проект в чистом окружении… Развёрнутая версия
является преимуществом, но **не заменяет** возможность запуска проекта из репозитория».

| | Якорь | Проверка |
|---|---|---|
| 20 | `git clone` → `cp .env.example .env` (вписать один ключ) → `docker compose up -d --build` → сценарий проходит. **Проверено на чистой машине или в чистом volume**, не на машине автора. Плюс живая ссылка | лог прогона в `docs/progress/`, скриншот; без чистого прогона — потолок 12 |
| 12 | поднимается из репозитория, но нужны недокументированные шаги; либо прогон только на машине автора | |
| 6 | работает только прод, из репозитория не поднимается | |
| 0 | не поднимается | |

**Обязательно:** `.env.example` со всеми переменными · фиксированные версии образов и зависимостей ·
данные для демо создаются скриптом из репозитория (`seed`), а не лежат «где-то у нас».

### Базовая надёжность и безопасность — 15
Дословно: «основной сценарий не завершается ошибкой при корректных входных данных;
предусмотрена обработка очевидных некорректных запросов».

| | Якорь | Проверка |
|---|---|---|
| 15 | сценарий прогнан **10 раз подряд** без падения; пустой ввод, мусорный ввод, слишком длинный ввод и отсутствующий id дают понятную ошибку 4xx, а не 500; секретов в репозитории нет | скрипт прогона + 4 негативных curl'а в README; `git log -p | grep -E "sk-[A-Za-z0-9]"` пуст |
| 10 | сценарий стабилен, негативные случаи частично | |
| 5 | падает на мусорном вводе 500-й | |
| 0 | нестабилен на корректном вводе | |

## Этап 2 — Demo Day (включается после freeze, для подготовки к 29.09)

| Критерий | Макс | 100 % | 60 % |
|---|---|---|---|
| Ценность решения | 25 | боль с цифрой в первые 20 секунд; польза очевидна из демо без пояснений | боль названа, польза объясняется словами |
| Результат и качество решения | 20 | живое демо проходит сценарий до действия без сбоя | демо с запинкой или по скриншотам |
| Потенциал развития и масштабирования | 20 | конкретные шаги, куда масштабируется, что нужно для пилота | общие слова |
| Презентация, демо и ответы | 20 | 5 слайдов, укладываемся во время, каждый готов сказать про свой вклад (п. 9.3) | слайды есть, роли не распределены |
| Инновационность | 15 | названа очевидная альтернатива и чем мы отличаемся | отличие не сформулировано |

Три судьи минимум, итог — среднее арифметическое (п. 9.5).

## Потолки без подтверждения человеком
- README ≤ 12, пока другой человек не повторил проверку сценария по README.
- Воспроизводимость ≤ 12, пока не было прогона в чистом окружении.
- Соответствие кейсу ≤ 14, пока сценарий не прошли с чужого устройства.
