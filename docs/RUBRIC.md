# Рубрика · **v3** · 23.09 13:45 · по критериям ТЗ задачи «Agentic AI для прогнозирования выработки ВЭС»

Положение от 22.09, п. 5.5: критерии задаёт ТЗ задачи. v2 (общие критерии) недействительна.
Правила изменения прежние: меняет Абылай; ослабить якорь — только со ссылкой на ТЗ; якорь должен проверяться
командой или скриншотом. **Условия допуска (не баллы, а всё или ничего):** проект запускается по README из
чистого клона (п. 5.4.16) · проверка без личных аккаунтов (п. 5.6.6) · коммит в каждый отчётный час (п. 5.4.8).

## Соответствие задаче и работоспособность — 25
| | Якорь | Проверка |
|---|---|---|
| 25 | **29 выпусков** 31.01–28.02, по 48 ч × {1, 2, plant}, каждый прошёл полный агентный цикл; правило «без будущего» соблюдено; пересчёт при новом прогоне виден (v1 → v2); сценарий проходит на проде с чужого устройства | `verify.py` PASS; `outputs/forecasts/` 29 файлов; прогон с телефона |
| 18 | всё есть, но пересчёт не реализован **или** проверка утечки частичная | |
| 12 | прогнозы есть, но погода не point-in-time **или** не все даты | |
| 6 | один выпуск вручную | |

## Техническая реализация — 25
| | Якорь | Проверка |
|---|---|---|
| 25 | агент сам выбирает порядок 6 инструментов, последний — `publish_forecast` (action); числа — только модель; квантили P10/P50/P90; **бьёт персистентность и климатологию на январе по nMAE**; кэш погоды; детерминированный режим без ключа | трейс; `GET /api/metrics`; код инструментов |
| 18 | работает, но агент — жёсткий конвейер без решений **или** не бьёт базовые линии | |
| 12 | модель есть, агентного слоя нет | |
| 6 | числа генерирует LLM | |

## README и воспроизводимость — 25
| | Якорь | Проверка |
|---|---|---|
| 25 | README содержит все 8 пунктов п. 5.4.15 + методику, **контроль утечки**, метрики января с базовыми линиями, известные ограничения; `git clone` → `.env` → `docker compose up` → `verify.py` PASS **на чистой машине, проверял не автор**; работает без ключа и без сети (кэш) | лог `docs/progress/clean-run.md` |
| 15 | всё есть, чистый прогон не подтверждён человеком (**потолок без подтверждения**) | |
| 8 | запуск описан, методики и проверки нет | |
| 0 | не запускается — недопуск | |

## Ценность и применимость — 15
| | Якорь |
|---|---|
| 15 | README: кто пользуется (диспетчер ВЭС, заявка на сутки вперёд), чем больно (небаланс, штрафы, резерв), что даёт интервал P10–P90, **цифра выигрыша против базовой линии** |
| 8 | польза описана без цифр |

## Потенциал развития и оригинальность — 10
| | Якорь |
|---|---|
| 10 | 2–3 обоснованных нестандартных хода явно названы (point-in-time как свойство системы, пересчёт по событию нового прогона, квантили, ансамбль погодных моделей) + масштабирование: новая ВЭС = координаты + история |
| 5 | общие слова |

## Потолки без подтверждения человеком
README и воспроизводимость ≤ 15 · соответствие задаче ≤ 18 (нет прогона с чужого устройства).

## Demo Day (29.09) — по положению, п. 5.7.2
Ценность 25 · Результат 20 · Инновационность 15 · Потенциал 20 · Презентация 20.
