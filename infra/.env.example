# Copy to repo root as .env (gitignored). Same file on the server.
DOMAIN=pushin.codes
OPENAI_API_KEY=sk-...
# On a host that already serves 80/443 through its own web server:
#   DEPLOY_COMPOSE_FILE=infra/docker-compose.hosted.yml
# Optional
LOG_LEVEL=info
