# Automatic TLS via Let's Encrypt. DOMAIN comes from .env.
{$DOMAIN} {
	encode gzip
	handle /api/* {
		reverse_proxy backend:8000 {
			flush_interval -1          # SSE: stream immediately
		}
	}
	handle /health {
		reverse_proxy backend:8000
	}
	handle {
		reverse_proxy frontend:3000
	}
}
