# Windcast — агент прогноза выработки ВЭС на 48 часов

**Windcast** — Agentic AI-система для диспетчера ветроэлектростанции из двух турбин (43.645, 78.536).
К каждому моменту выпуска агент сам получает по координатам ВЭС архивный прогноз погоды из открытого
источника (Open-Meteo, модель ECMWF IFS). Берёт он только прогоны, опубликованные до этого момента. Дальше
агент проверяет данные, запускает модель и получает почасовой прогноз выработки на 48 часов с интервалом
P10–P90 для каждой турбины и для ВЭС в целом. Результат он анализирует (рампы, обледенение, ветер > 20 м/с),
при выходе нового прогона погоды сам решает, нужен ли пересчёт, и публикует выпуск: CSV, сводку для
диспетчера и ленту своих шагов. Февраль 2026 воспроизведён день за днём, как если бы прогноз делался
в прошлом: **29 выпусков, с 31.01 по 28.02**. В режиме Live агент без участия человека проверяет выход
новых прогонов и ведёт журнал своих решений.

**Демо без регистрации:** https://pushin.codes · **Трек:** «Энергетика», HackAlem AI, 23.09.2026 ·
**Команда:** Pushin' Code

## Быстрый старт

```bash
git clone https://github.com/BAITC-Hacks/hack-07de2d2d-pushin-code && cd hack-07de2d2d-pushin-code
cp .env.example .env              # необязательно: без OPENAI_API_KEY агент работает детерминированно
docker compose up --build         # первая сборка — несколько минут; затем открыть http://localhost:8080
curl http://localhost:8080/health # в другом терминале: {"ok":true,…,"issues_ready":29}
python3 scripts/verify.py         # из корня клона, только стандартная библиотека → PASS
```

**Как проверить за 3 минуты** — [§ 8](#8-проверка-основного-сценария). Без Docker —
[§ 6.3](#63-без-docker-для-разработчика). Что проверяет `verify.py` — [§ 4](#4-правило-без-будущего).

**Содержание:** [1 Описание](#1-описание-решения-и-назначение) ·
[2 Соответствие задаче](#2-как-это-соответствует-задаче) · [3 Архитектура](#3-архитектура) ·
[4 Без будущего](#4-правило-без-будущего) · [5 Технологии](#5-технологии-и-зависимости) ·
[6 Установка и запуск](#6-установка-и-запуск) · [7 Окружение](#7-параметры-окружения) ·
[8 Проверка](#8-проверка-основного-сценария) · [9 Результаты](#9-результаты) ·
[10 Структура](#10-структура-репозитория) · [11 Сторонние компоненты](#11-сторонние-компоненты-и-заготовки) ·
[12 Ограничения](#12-ограничения-и-потенциал-развития) · [13 Команда](#13-команда)

---

## 1. Описание решения и назначение

**Для кого.** Для диспетчера ВЭС. Каждый день он заявляет почасовой график выработки на следующие сутки,
а отклонение факта от графика даёт небаланс: его покрывают резервом и за него платят. Выработка ветровой
станции целиком зависит от погоды, поэтому одной цифры диспетчеру мало. Ему нужны ещё оценка
неопределённости и предупреждения о рискованных часах.

**Что получает диспетчер** в каждом выпуске — 48 часов × {турбина 1, турбина 2, ВЭС}:

- **P50** — ожидаемая выработка в долях номинала;
- **коридор P10–P90**: по построению факт попадает в него примерно в 80 % часов (на январе —
  `TODO(16:15)` %). P10 — осторожная оценка для заявки, а ширина коридора показывает риск;
- **флаги на оси времени**: рампа (изменение ≥ 30 % номинала за 2 ч), обледенение (температура от −3 до
  +1 °C не меньше 2 ч подряд), ветер > 20 м/с, расхождение погодных моделей, скачок против прошлой версии;
- **сводку** простым языком и строку «что изменилось», если прогноз пересчитан;
- **CSV выпуска** для своих систем.

Выигрыш на январе 2026: nMAE модели `TODO(16:15)` % против `TODO(16:15)` % у лучшей простой базовой линии,
то есть ошибка меньше на `TODO(16:15)` % (подробности в [§ 9](#9-результаты)).

**Что агент делает сам.** Полный цикл из ТЗ, этап за этапом:

1. **Получение внешних погодных данных** — `fetch_weather`: Open-Meteo по координатам ВЭС, только прогоны,
   опубликованные до момента выпуска. Без сети берёт сохранённые ответы из `data/weather_cache/`.
2. **Подготовка данных** — `check_data`: 48 часов без пропусков, значения конечные, все прогоны
   опубликованы до момента выпуска.
3. **Запуск модели** — `run_model`.
4. **Формирование почасового прогноза**: P10/P50/P90 × 48 ч × {1, 2, ВЭС}.
5. **Анализ результата** — `analyze_forecast`: флаги рисков и сводка.
6. **Повторный расчёт при обновлении входных данных** — `recalc_forecast`. Когда выходит более свежий
   прогон, агент сравнивает его с прежним и решает сам: пересчитать (версия v2) или объяснить, почему
   пересчёт не нужен. Если прогон вышел бы после момента выпуска, код откажет в пересчёте.

Цикл завершается действием `publish_forecast`: агент записывает CSV, запись выпуска и ленту шагов.
Порядок шагов и решения — за LLM (OpenAI, function calling), числа прогноза считает только модель
(кто что решает — [§ 3.2](#32-где-здесь-agentic-ai)).

**Без человека.** В режиме Live бэкенд сам будит агента раз в 30 минут: тот проверяет, вышел ли новый
прогон, решает, выпускать ли новую версию, и записывает итог в журнал (`GET /api/live/journal`, вкладка
«Live»). Если источник погоды отказал, агент повторяет запрос, переходит на запасной путь и предупреждает
о сниженном доверии к выпуску. `TODO(16:15)`: проверить наблюдатель, журнал и сценарий сбоя через API.

**Демо-доступ (п. 5.6.6).** https://pushin.codes открывается без регистрации и личных аккаунтов. Агент там
работает в режиме LLM на ключе OpenAI команды. Для локального запуска ключ не нужен: без него агент
проходит тот же цикл в детерминированном режиме и выдаёт те же числа.

`TODO(16:15)`: 1–2 скриншота (календарь февраля и панель агента) → `docs/screenshots/`, вставить сюда.

## 2. Как это соответствует задаче

Текст ТЗ — [`docs/hackathon/task.md`](docs/hackathon/task.md).

| Требование ТЗ | Как сделано | Где в коде | Как проверить |
|---|---|---|---|
| Модель почасовой выработки на исторических данных | SCADA 10 мин → часовой ряд в UTC с флагом `valid` (пропуски, простои и отключение Т1 в 05–07.2024 исключены). Квантильная модель P10/P50/P90 учится на архивных прогнозах погоды против фактической мощности; для сравнения есть три базовые линии | `backend/windcast/data.py`, `model.py` `TODO(16:15)` | `python -m windcast.data`; вкладка «Качество»; `outputs/metrics_jan.json` |
| Сама получает по координатам ВЭС из открытых источников прогнозы погоды, доступные на момент прогнозирования | `fetch_weather` сам запрашивает Open-Meteo (Previous Runs API, ECMWF IFS 0,25°) и берёт только прогоны, опубликованные до момента выпуска T ([§ 4](#4-правило-без-будущего)). Источник `api` или `cache` пишется в каждое событие | `tools.py`, `weather.py` `TODO(16:15)`, `timeline.py` | кнопка «✓ Без будущего»; `meta.source` в ленте шагов |
| Прогноз на 24–48 ч с почасовой детализацией | 48 часов: h = 1…48, то есть (D+1) 00:00 … (D+2) 23:00 по UTC+5, для турбин 1, 2 и ВЭС, P10/P50/P90 | `outputs/forecasts/{D}.csv` ([§ 9](#9-результаты)) | `verify.py`: 144 строки в каждом выпуске |
| Agentic AI: система сама выполняет полный цикл | Агент с шестью инструментами: LLM выбирает следующий шаг, каждый шаг — событие с этапом `meta.stage`. В Live агент запускается сам по расписанию и ведёт журнал | `agent.py`, `tools.py`; Live-наблюдатель `TODO(16:15)` | панель агента: этапы загораются по очереди; `outputs/traces/{D}.jsonl`; журнал агента во вкладке «Live» |
| → получение внешних погодных данных | `fetch_weather` | `tools.py`, `ports.py` | этап «погода» (`meta.stage = weather`) |
| → подготовка данных | `check_data` | `tools.py`, `data.py` | этап «подготовка» (`prep`) |
| → запуск модели | `run_model` | `tools.py`, `model.py` | этап «модель» (`model`) |
| → формирование почасового прогноза | результат `run_model`: 144 строки P10/P50/P90 | `tools.py`, `store.py` | этап «прогноз» (`forecast`) |
| → анализ результата | `analyze_forecast`: флаги рисков и сводка | `tools.py` | этап «анализ» (`analysis`) |
| → повторный расчёт при обновлении входных данных | вышел новый прогон → агент решает → `recalc_forecast` → v2 или «пересчёт не нужен» | `agent.py`, `tools.py` | этап «пересчёт» (`recalc`); «↻ Перевыпустить агентом», «⚡ Новый прогон погоды» |
| Воспроизвести, как в прошлом: 31.01, затем 01.02 и далее последовательно | Выпуск D делается в момент T = (D+1) 00:00 местного. D = 31.01 … 28.02 — 29 выпусков подряд, каждый проходит полный цикл агента | `backtest.py`, `timeline.issue_dates()` | календарь из 29 выпусков; «▶ Воспроизвести февраль»; `verify.py` |
| Только архивные прогнозы, доступные на тот момент, а не факт | Правило `init + 8 ч ≤ T`. В признаках нет факта, реанализа и SCADA. В каждой строке CSV есть `weather_init_max_utc` | `timeline.py`, `tools.py`, `scripts/verify.py` | `python3 scripts/verify.py` → «Без будущего» ✓ |

Каждый день 01–28.02 покрыт выпуском накануне (h 1–24), а начиная с 02.02 ещё и выпуском за двое суток
(h 25–48). Выпуск от 28.02 (на 01–02.03) закрывает второе прочтение ТЗ: «повторить прогнозирование
в течение тестового периода».

## 3. Архитектура

```
Браузер ──► Caddy   (локально http://localhost:8080 · прод https://pushin.codes)
              ├── /                ──► frontend :3000   экраны «Февраль 2026», «Live», «Качество»
              └── /api/*, /health  ──► backend  :8000   FastAPI: REST + SSE-поток шагов агента
                                             │
   кнопка или POST /api/runs,                ▼
   CLI windcast.backtest,       ──►  агент: ≤ 8 вызовов инструментов ◄──► OpenAI API
   Live-наблюдатель раз в 30 мин     LLM выбирает шаг; без ключа — те же шаги по правилам
                                             │ вызывает
      fetch_weather ─────► Open-Meteo API по координатам ВЭС (без сети — data/weather_cache/)
      check_data ────────► 48 ч без пропусков, все прогоны опубликованы до момента выпуска T
      run_model ─────────► models/*.pkl → P10/P50/P90 × 48 ч × {турбина 1, турбина 2, ВЭС}
      analyze_forecast ──► рампы, обледенение, ветер > 20 м/с, скачок против прошлой версии
      recalc_forecast ───► вышел более свежий прогон → пересчёт → v2; поздний прогон — отказ кода
      publish_forecast ──► действие: outputs/forecasts/{D}.csv + {D}.json, outputs/traces/{D}.jsonl
                                             │
      API и экран читают только эти файлы ◄──┘  (февраль открывается уже посчитанным)
```

Подробная схема с диаграммой последовательности — [`docs/architecture.html`](docs/architecture.html)
(открыть в браузере). Спецификация API и форматов — [`docs/CONTRACT.md`](docs/CONTRACT.md). Swagger —
http://localhost:8080/api/docs.

Модули бэкенда: `agent.py` — цикл агента, `tools.py` — шесть инструментов и их JSON-схемы для OpenAI,
`store.py` — атомарная запись выпусков, `ports.py` — подключение функций данных, погоды и модели,
`runs.py` — прогоны в фоне и SSE, `api.py` — REST, `backtest.py` — CLI.

### 3.1 Цикл агента

1. Прогон запускают кнопка «↻ Перевыпустить агентом», `POST /api/runs`, CLI `windcast.backtest` или
   Live-наблюдатель. Бэкенд ведёт прогон в фоновом потоке и отдаёт каждое событие на экран по SSE
   (`GET /api/runs/{id}/events`).
2. **v1.** `fetch_weather(run="previous")` берёт прогон на сутки старше. Затем `check_data`, `run_model`,
   `analyze_forecast` и `publish_forecast`, который публикует v1.
3. **Вышел новый прогон.** `fetch_weather(run="latest")` возвращает не решение, а факты: свежее ли прогон,
   опубликован ли он до T, средний сдвиг ветра за часы 1–24, появились ли новые риски. Решение принимает
   агент. Политика: пересчитывать, если прогон свежее, опубликован до T и ветер сдвинулся в среднем больше
   чем на 0,5 м/с или появился новый риск. Тогда `recalc_forecast` считает v2 со строкой «что
   изменилось», а `publish_forecast` её публикует. Иначе агент объясняет, почему v1 остаётся.
4. **«⚡ Новый прогон погоды»** (`trigger = new_weather_run`): агент ищет прогон свежее текущего
   и опубликованный до T. На архивном дне, где v2 уже учла последний такой прогон, его нет, и агент
   объясняет: «Более свежего прогона до момента выпуска нет — v2 остаётся».
5. **Сбой источника** (`scenario = weather_outage` имитирует отказ Open-Meteo на первом запросе). Агент
   повторяет запрос, продолжает на запасном пути и помечает выпуск предупреждением «доверие к выпуску ниже
   обычного».
6. Лимит — 8 вызовов инструментов. Последнее событие всегда `verdict`. События идут по схеме
   [`docs/references/trace-event-schema.md`](docs/references/trace-event-schema.md); у каждого есть
   `meta.stage`, `meta.status`, `meta.source`, у решений — `meta.by`.

### 3.2 Где здесь agentic AI

| Кто | Что делает |
|---|---|
| **Модель** | Считает все числа: P10/P50/P90 на каждый час и турбину. LLM числа не придумывает и не правит: `publish_forecast` принимает сводку и номер версии, а не строки прогноза. |
| **Код** | Гарантирует правило «без будущего» (`init + 8 ч ≤ T`): `check_data` не пропускает прогон, вышедший после T, а `recalc_forecast` отказывает в пересчёте на позднем прогоне, что бы ни попросила LLM. Заменяет сводку LLM шаблонной, если в ней есть числа, которых нет в ответах инструментов. Держит лимит 8 вызовов. Если LLM недоступна, ошиблась или остановилась раньше, доводит цикл по тем же правилам и пишет об этом в ленту. По одной дате одновременно идёт только один прогон. |
| **LLM** (OpenAI, function calling, по умолчанию `gpt-5-mini`) | Выбирает порядок шагов. Решает, пересчитывать ли после нового прогона, и пишет причину в ленту (`meta.by = "llm"`). Решает, как реагировать на сбой источника погоды: повторить запрос или продолжить с предупреждением. Пишет сводку для диспетчера. |
| **Без ключа** | Тот же цикл идёт по правилам (`meta.by = "rule"`): та же политика пересчёта, сводка по шаблону, те же числа. |

**Агент работает без человека.** Бэкенд раз в `LIVE_WATCH_MINUTES` минут (по умолчанию 30, `0` —
выключено; первая проверка — примерно через 10 секунд после старта) запускает агента для Live. Если
выпуска ещё нет, агент делает его, а если есть — проверяет новый прогон и сам решает, выпускать ли новую
версию. Каждый запуск Live, свой и по кнопке, пишется в `outputs/live/journal.jsonl`. Журнал отдаёт
`GET /api/live/journal`: кто запустил (`agent` или `user`), итог (`published`, `kept`, `refused`, `error`)
и фраза агента, например «Вышел прогон 06 UTC, сдвиг 1,1 м/с — выпустил v2». Это главное доказательство
автономности. `TODO(16:15)`: проверить наблюдатель и журнал после слияния (в `api.py` этого дерева их ещё
нет).

**Детерминированный режим.** Без `OPENAI_API_KEY` (или с `"mode": "deterministic"`) агент идёт по тем же
шагам и выдаёт те же числа. Текущий режим показывает `GET /health` → `mode`. Если ключа нет, запрос
с `"mode": "agent"` тоже выполняется детерминированно, и первое событие в ленте об этом говорит.

**Live.** Для `issue_date = "live"` момент выпуска T — «сейчас». Погода берётся из Open-Meteo Forecast
API (последний вышедший прогон), окно — 48 ч от следующего полного часа.

### 3.3 Модель и методика

`TODO(16:15)`: сверить с `model.py` и `evaluate.py` (алгоритм, итоговые признаки, файлы в `models/`,
`MODEL_VERSION`).

- **Данные.** SCADA двух турбин, 10-минутные записи с 11.03.2023 по 31.01.2026 23:50, превращаются
  в часовые средние в UTC (`python -m windcast.data` → `data/processed/hourly.parquet`). Час помечается
  `valid = false`, если он неполный (меньше 6 записей), если это простой (мощность меньше 0,02 при ветре
  больше 5 м/с) или если он попал в 18.05–17.07.2024 у турбины 1 (≈ 60 дней пропусков). Валидных часов:
  турбина 1 — 92,4 %, турбина 2 — 97,0 %. Разбор данных — [`docs/DATA_NOTES.md`](docs/DATA_NOTES.md).
- **Признаки** — только прогноз погоды и календарь: ветер на 100 м (и на 10 м), ветер³, направление
  (sin/cos), температура, час, месяц, горизонт h, турбина. SCADA в признаках нет: в феврале её нет.
- **Обучение на тех же лагах, что и прогноз.** Признаки из `*_previous_day1/2` сопоставляются
  с фактической мощностью (архив Previous Runs доступен примерно с 06.2024). Так модель заодно учит ошибку
  погодной модели на той же заблаговременности, с которой работает в прогнозе.
- **Выход:** квантили P10/P50/P90 (`TODO(16:15)`: LightGBM с квантильной функцией потерь?), обрезка в [0, 1],
  упорядочивание P10 ≤ P50 ≤ P90. ВЭС — среднее нормированной мощности двух турбин.
- **Окна обучения.** Для бэктеста января — данные до 01.01.2026. Для февраля — всё до 31.01.2026 включительно.
- **Базовые линии:** персистентность («завтра как сегодня», последние 24 ч факта), климатология (среднее по
  часу × месяцу), кривая мощности (медианная кривая SCADA, применённая к прогнозному ветру).
- **Метрики:** nMAE и nRMSE. Мощность нормирована, поэтому ошибка выражена в % номинала. Считаем по всем
  часам и по горизонту, плюс покрытие коридора P10–P90.
- **Часы SCADA.** Сдвиг к UTC постоянный, 5 или 6 часов (`SCADA_UTC_OFFSET_H`). Выбран по январскому бэктесту:
  `TODO(16:15)`.

## 4. Правило «без будущего»

Это главная проверка задачи: прогноз на прошлую дату нельзя строить на погоде, которую тогда ещё не знали.

- **Момент выпуска.** Выпуск D делается в момент **T = D 19:00 UTC = (D+1) 00:00 по UTC+5**, прогноз — на
  48 часов после T. Внутри системы всё время в UTC, на экране и в файлах — местное UTC+5.
- **Прогон виден не сразу.** ECMWF запускает прогоны в 00, 06, 12 и 18 UTC, а открытые данные публикует
  примерно через 7–8 часов. Мы считаем прогон доступным через **8 часов после старта**:
  **`init + 8 ч ≤ T`**, то есть старт не позже D 11:00 UTC. Самый свежий допустимый прогон — **D 06:00 UTC**
  (опубликован ≈ D 14:00 UTC). Прогоны D 12:00 UTC (≈ D 20:00 UTC) и D 18:00 UTC (≈ D+1 02:00 UTC)
  стартовали раньше T, но вышли уже после него, поэтому запрещены.
- **Какой прогон берём на каждый час.** Open-Meteo Previous Runs API отдаёт `*_previous_dayN` — значение
  из прогона, стартовавшего не позже чем за N суток до целевого часа. N подбирается по горизонту так,
  чтобы прогон был опубликован до T: `N = timeline.lead_days(h)` = ⌈(h − 1 + 8) / 24⌉.

  | Горизонт h | Целевые часы (UTC+5) | N (`previous_dayN`) | v1 (прогон на сутки старше) |
  |---|---|---|---|
  | 1–17 | (D+1) 00:00 … 16:00 | 1 | 2 |
  | 18–41 | (D+1) 17:00 … (D+2) 16:00 | 2 | 3 |
  | 42–48 | (D+2) 17:00 … 23:00 | 3 | 4 |

- **Запрещено в признаках:** `previous_day0`, фактическая погода, реанализ, SCADA.
- **У каждой строки CSV есть `weather_init_max_utc`** — верхняя граница старта прогона: целевой час минус
  N суток, округлённый вниз до 00/06/12/18 UTC.
- **Агент это правило обойти не может.** LLM выбирает только `run = "previous"` или `"latest"`, а время
  старта вычисляет код. `check_data` отклоняет погоду с прогоном позже T, `recalc_forecast` отказывает
  в пересчёте на таком прогоне.

**Пример: выпуск D = 13.02.2026**, T = 13.02 19:00 UTC = 14.02 00:00 по UTC+5 (рассчитано `timeline.py`):

| h | Целевой час (UTC+5) | N | `weather_init_max_utc` | Опубликован ≈ | ≤ T? |
|---|---|---|---|---|---|
| 1 | 14.02 00:00 | 1 | 2026-02-12T18:00Z | 13.02 02:00 UTC | да |
| 17 | 14.02 16:00 | 1 | 2026-02-13T06:00Z | 13.02 14:00 UTC | да |
| 18 | 14.02 17:00 | 2 | 2026-02-12T12:00Z | 12.02 20:00 UTC | да |
| 41 | 15.02 16:00 | 2 | 2026-02-13T06:00Z | 13.02 14:00 UTC | да |
| 42 | 15.02 17:00 | 3 | 2026-02-12T12:00Z | 12.02 20:00 UTC | да |
| 48 | 15.02 23:00 | 3 | 2026-02-12T18:00Z | 13.02 02:00 UTC | да |

**Как это доказывает `scripts/verify.py`.** Скрипт работает на стандартной библиотеке Python ≥ 3.9 и
намеренно **не импортирует** `timeline.py`: правила времени записаны в нём заново, чтобы проверка не
повторяла ошибки проверяемого кода. Для каждого из 29 выпусков он проверяет:

- в **каждой строке** CSV: `weather_init_max_utc + 8 ч ≤ T`;
- в записи `{D}.json`: у **каждого** прогона погоды **каждой** версии (v1 тоже) `init_utc + 8 ч ≤ T`;
- формат по § 7 контракта: столбцы по порядку, 144 строки = 48 ч × {1, 2, plant}, время в +05:00, одна
  версия в файле, P10 ≤ P50 ≤ P90 в [0, 1], ветер в [0, 60) м/с;
- ленту шагов `{D}.jsonl`: события по схеме, есть действие (`action`), последнее событие — `verdict`;
- что нигде нет заглушек (`source = stub`), что сводный CSV совпадает с файлами выпусков и что nMAE модели
  на январе ниже каждой из трёх базовых линий.

Код возврата 0 — PASS, 1 — FAIL. При ошибке печатается `файл:строка · что не так`. В интерфейсе то же
правило видно по кнопке «✓ Без будущего»: какой прогон взят на какие часы и когда он вышел.

## 5. Технологии и зависимости

| Слой | Технология | Версия |
|---|---|---|
| Язык бэкенда | Python | 3.12 (образ `python:3.12-slim`) |
| API | FastAPI, Uvicorn; SSE через Starlette `StreamingResponse` | 0.115.12, 0.34.0 |
| Данные | pandas, NumPy, PyArrow (Parquet) | 2.2.3, 2.2.2, 19.0.1 |
| Модель | LightGBM, scikit-learn, SciPy, joblib | 4.6.0, 1.6.1, 1.15.2, 1.4.2 |
| Агент | openai-python (Chat Completions, function calling), модель `gpt-5-mini` | 1.109.1 |
| HTTP-клиенты | requests, HTTPX | 2.32.3, 0.28.1 |
| Тесты и линтер | pytest, Ruff | 8.3.5, 0.16.8 |
| Сборка пакета | build, setuptools | 1.2.2.post1, 75.8.0 |
| Интерфейс | `TODO(16:15)`: стек Сулы, библиотека графиков, lock-файл | |
| Прокси | Caddy (образ `caddy:2.11.4-alpine`) | 2.11.4 |
| Запуск | Docker, Docker Compose v2 | |

Все версии Python-пакетов зафиксированы через `==` в [`backend/requirements.txt`](backend/requirements.txt),
его ставит и Docker-образ.

**Внешние сервисы:**

| Сервис | Зачем | Ключ | Без него |
|---|---|---|---|
| [Open-Meteo Previous Runs API](https://open-meteo.com/en/docs/previous-runs-api) | архивные прогнозы из прошлых прогонов (`*_previous_dayN`) для выпусков и обучения | не нужен, бесплатно | ответы из `data/weather_cache/` (`meta.source = "cache"`) |
| [Open-Meteo Forecast API](https://open-meteo.com/en/docs) | прогноз для Live | не нужен, бесплатно | Live не работает, архивный февраль работает |
| [OpenAI API](https://platform.openai.com/) | выбор шагов агента, решения о пересчёте, тексты сводок | `OPENAI_API_KEY`, **необязателен** | детерминированный режим, те же числа |

`TODO(16:15)`: сверить с `weather.py`, используется ли ещё Historical Forecast API.

## 6. Установка и запуск

### 6.1 Системные требования

- Docker с Compose v2 (команда `docker compose`, не `docker-compose`), например Docker Desktop. Образ
  backend проверен на Docker 29.6 / Compose 5.2, arm64 (Apple Silicon). `TODO(16:15)`: отметить проверку
  всего стека на amd64 (сервер).
- Свободные порты **8080** (интерфейс и API через прокси), **8000** (API напрямую), **3000** (фронтенд
  напрямую). Если порт занят, поменяйте левую часть в `ports` в `docker-compose.yml`, например `"8081:8080"`.
- Около 1,5 ГБ на диске под образы (образ backend — ≈ 0,7 ГБ; `TODO(16:15)`: размер образа frontend)
  и около 1 ГБ свободной памяти.
- Интернет — для сборки образов (PyPI, Docker Hub) и для погоды Open-Meteo. Без сети при работе агент берёт
  погоду из `data/weather_cache/`.
- Для `scripts/verify.py` на хосте нужен только `python3` ≥ 3.9, без пакетов. В Windows вместо `python3`
  используйте `python` или `py -3`.

### 6.2 Docker, основной путь

```bash
git clone https://github.com/BAITC-Hacks/hack-07de2d2d-pushin-code
cd hack-07de2d2d-pushin-code
cp .env.example .env         # необязательно; для режима LLM впишите OPENAI_API_KEY=…
docker compose up --build    # или docker compose up --build -d, чтобы вернуть терминал
```

- Файл `.env` можно не создавать: compose читает его только для подстановки переменных и без него тоже
  стартует.
- Поднимаются три контейнера: `backend` (FastAPI, собирается из корня репозитория, потому что образу нужны
  `data/`, `models/` и `outputs/`), `frontend` и `proxy` (Caddy).
- Готовность: `curl http://localhost:8080/health` →
  `{"ok":true,"mode":"deterministic","model_version":"…","issues_ready":29}`.

| Адрес | Что |
|---|---|
| http://localhost:8080 | интерфейс, `/api/*` и `/health` через один прокси — **открывать это** |
| http://localhost:8080/api/docs | Swagger UI со всеми эндпоинтами |
| http://localhost:8000 | API напрямую |
| http://localhost:3000 | фронтенд напрямую, без прокси |

Февраль уже посчитан: 29 выпусков из `outputs/` попадают в образ при сборке. Если вы перегенерировали
выпуски на хосте ([§ 8.2](#82-из-командной-строки)), пересоберите образ: `docker compose up --build`.

**Остановить:** `Ctrl+C`, затем `docker compose down`. Удалить и собранные образы:
`docker compose down --rmi local`.

`TODO(16:15)`: в этом дереве ещё нет `frontend/Dockerfile`. Без него `docker compose up --build` не соберётся —
проверить чистый клон после слияния интерфейса.

### 6.3 Без Docker (для разработчика)

Нужен Python 3.12. Все команды — из корня клона:

```bash
python3.12 -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r backend/requirements.txt                 # macOS: LightGBM нужен OpenMP — brew install libomp
export PYTHONPATH=backend                               # Windows PowerShell: $env:PYTHONPATH="backend"
python -m windcast.data                                 # SCADA → data/processed/hourly.parquet (секунды)
uvicorn windcast.api:app --port 8000                    # один воркер: прогоны агента живут в памяти процесса
```

`python -m windcast.data` печатает число часов и долю валидных по каждой турбине:
`turbine 1: hours=25392 valid_fraction=0.9242`, `turbine 2: hours=25392 valid_fraction=0.9704`.

Проверка: `curl http://localhost:8000/health`.

Интерфейс в режиме разработки: `TODO(16:15)`: команды Сулы (установка по lock-файлу, dev-сервер на :3000
с прокси `/api` → :8000).

Обучение модели и метрики января: `TODO(16:15)`: команды Кубы — обучение (если модель не только из
`models/`) и `python -m windcast.evaluate --from 2025-12-31 --to 2026-01-29`.

### 6.4 Тесты

```bash
python -m pytest -q backend/tests    # агент, инструменты, запись выпусков, API, SCADA, время; без сети
python -m pytest -q scripts/tests    # verify.py на синтетических выпусках, запуск как у жюри
```

Сейчас это 86 и 32 теста. `TODO(16:15)`: обновить числа после слияния погоды и модели.

## 7. Параметры окружения

Все переменные **необязательны**, проект запускается и без `.env`. Секреты хранятся только в `.env`, он
в `.gitignore`. Шаблон — [`.env.example`](.env.example).

| Переменная | По умолчанию | Где читается | Что делает |
|---|---|---|---|
| `OPENAI_API_KEY` | пусто | `api.py`, `agent.py`, `backtest.py` | есть ключ → режим `agent` (шаги и решения — LLM); нет → `deterministic` |
| `OPENAI_MODEL` | `gpt-5-mini` | `agent.py` | модель OpenAI для агента |
| `OPENAI_REASONING_EFFORT` | `low` | `agent.py` | глубина рассуждений для моделей `gpt-5*`; пустое значение — не передавать |
| `OPENAI_TIMEOUT_S` | `60` | `agent.py` | таймаут запроса к OpenAI, секунд |
| `OPENAI_BASE_URL` | API OpenAI | openai-python | другой адрес OpenAI-совместимого API |
| `LIVE_WATCH_MINUTES` | `30` | Live-наблюдатель (`TODO(16:15)`: проверить) | как часто агент сам проверяет новые прогоны для Live; `0` — выключено |
| `WINDCAST_ROOT` | корень репозитория; в образе `/app` | `paths.py` | откуда читать и куда писать `data/`, `models/`, `outputs/` |
| `SCADA_UTC_OFFSET_H` | `5` | `config.py` | сдвиг часов SCADA к UTC: `5` или `6`, другое значение — ошибка |
| `WINDCAST_FORCE_STUBS` | пусто | `ports.py` | `1` — синтетические заглушки вместо погоды и модели (только тесты, без сети) |
| `PYTHONPATH` | в образе `/app/backend` | Python | для запуска без Docker: `backend`, чтобы работало `python -m windcast.…` |

Корневой `docker-compose.yml` передаёт в контейнер `OPENAI_API_KEY` и `OPENAI_MODEL`. `TODO(16:15)`:
добавить в compose и `.env.example` `LIVE_WATCH_MINUTES` и `OPENAI_REASONING_EFFORT` (или убрать эту
строку). Остальное задано в образе (`backend/Dockerfile`) и нужно только при запуске без Docker.
Переменные `DOMAIN` и `DEPLOY_COMPOSE_FILE` из `infra/.env.example` нужны только серверу
https://pushin.codes, для проверки они не требуются.

## 8. Проверка основного сценария

### 8.1 В интерфейсе, за 3 минуты

Откройте https://pushin.codes (без регистрации) или локально http://localhost:8080. Тот же список есть
в самом интерфейсе — кнопка «Как проверить за 3 минуты» в шапке.

`TODO(16:15)`: сверить надписи и дату с готовым интерфейсом и реальными выпусками.

1. **Февраль посчитан.** Сразу открывается вкладка «Февраль 2026 · тест». В календаре 31.01 → 28.02 стоят
   29 выпусков: высота столбика — средняя выработка, точка — флаги риска, метка `v2` — был пересчёт.
   «▶ Воспроизвести февраль» проигрывает сохранённые ленты агента день за днём («запись прогона от …»).
2. **Выпуск за день.** Кликните 13.02 (`TODO(16:15)`: взять день с меткой `v2`). Появятся график на 48 часов
   (P50, коридор P10–P90, прошлая версия пунктиром, ветер, флаги) и «машина времени»: «Для системы сейчас
   13.02.2026 24:00 — всё правее она не видела». Переключатель ВЭС / Т1 / Т2.
3. **«✓ Без будущего».** Раскрывается список прогонов погоды: какой прогон взят на какие часы, когда он
   вышел, и что все вышли до момента выпуска.
4. **«↻ Перевыпустить агентом».** В панели справа по очереди загораются этапы ТЗ: погода · подготовка ·
   модель · прогноз · анализ · пересчёт. Лента шагов идёт вживую. Агент публикует v1, получает событие
   «вышел новый прогон погоды», сравнивает и решает. Если он пересчитал, то публикует v2 со строкой «что
   изменилось», а v1 уходит в пунктир. Внизу панели — сводка для диспетчера.
5. **«⚡ Новый прогон погоды».** Агент ищет прогон свежее текущего, опубликованный до T, и не находит:
   «Более свежего прогона до момента выпуска нет — v2 остаётся». В пояснении указано, когда стартовал
   следующий прогон, когда он вышел и что это позже T. Так правило «без будущего» соблюдается
   и в решении агента.
6. **«Live · сейчас».** «▶ Выпустить прогноз сейчас» запускает тот же цикл на последнем прогоне Open-Meteo
   Forecast API (нужен интернет). **«Журнал агента»** показывает, что агент сделал сам, без людей
   (`initiator = agent`). «Имитировать сбой погоды» — агент повторяет запрос и предупреждает.
   `TODO(16:15)`: проверить журнал и кнопку сбоя в готовом интерфейсе.
7. **«Качество».** Январь 2026: nMAE модели против трёх базовых линий, ошибка по горизонту и неделя
   «прогноз против факта». Факта за февраль нет — см. [§ 9](#9-результаты).

«⬇ CSV выпуска» и «⬇ Весь февраль CSV» скачивают файлы в формате [§ 9](#9-результаты).

### 8.2 Из командной строки

Перевыпустить февраль с нуля, посчитать метрики января и проверить результат (после
[§ 6.3](#63-без-docker-для-разработчика)):

```bash
export PYTHONPATH=backend
python -m windcast.backtest --from 2026-01-31 --to 2026-02-28 --mode deterministic
python -m windcast.evaluate --from 2025-12-31 --to 2026-01-29      # TODO(16:15): проверить
python3 scripts/verify.py
```

`backtest` печатает, откуда взяты погода, данные и модель, и по строке на выпуск
(`TODO(16:15)`: заменить реальным выводом):

```
backtest 2026-01-31 → 2026-02-28: 29 issues · mode=deterministic · weather=real, data=real, model=real
2026-01-31  v2  source=api  mode=deterministic  flags=ice:4  (…s)
…
combined: outputs/forecast_feb2026.csv (29 issues)
```

Что появится:

- `outputs/forecasts/{D}.csv` и `{D}.json` — 29 выпусков: CSV содержит последнюю версию, JSON — все версии;
- `outputs/traces/{D}.jsonl` — 29 лент шагов агента;
- `outputs/forecast_feb2026.csv` — сводный файл;
- `outputs/metrics_jan.json` — метрики января для вкладки «Качество» (пишет `evaluate`).

Выпуски в репозитории перезапишутся; что изменилось, покажет `git diff --stat outputs/`. Модель
детерминирована, поэтому числа должны совпасть с записанными, а тексты сводок могут отличаться, если
записанные выпуски сделаны в режиме LLM. `TODO(16:15)`: проверить и указать, в каком режиме записаны
выпуски в `outputs/` и сколько длится перевыпуск 29 дат с настоящей погодой.

То же внутри контейнера, если Python 3.12 ставить не хочется (`scripts/` в образ не входит, поэтому
проверка — на хосте):

```bash
docker compose exec backend python -m windcast.backtest --from 2026-01-31 --to 2026-02-28 --mode deterministic
mkdir -p /tmp/windcast-check && docker compose cp backend:/app/outputs /tmp/windcast-check/
python3 scripts/verify.py --root /tmp/windcast-check
```

API читает файлы заново на каждый запрос, поэтому интерфейс сразу покажет перевыпущенные выпуски.

Ожидаемый вывод `verify.py` (`TODO(16:15)`: заменить реальным):

```
Windcast · проверка выпусков 2026-01-31 … 2026-02-28 (29 выпусков)
…
  ✓ Выпуски на месте: forecasts/{D}.csv               29/29 выпусков
  ✓ Без будущего: прогон опубликован до T (CSV)       4176/4176 строк
  ✓ Без будущего: прогон опубликован до T ({D}.json)  …/… прогонов
  …
  ✓ Без заглушек: нигде нет source = stub             29/29 выпусков
  ✓ Сводный forecast_feb2026.csv = файлы выпусков     4176/4176 строк
  ✓ Январь: nMAE модели ниже всех базовых линий       nMAE: model … · power_curve … · climatology … · persistence …

PASS
```

### 8.3 API через curl

Работает и с `https://pushin.codes` вместо `http://localhost:8080`:

```bash
curl -s http://localhost:8080/health
curl -s "http://localhost:8080/api/issues?from=2026-01-31&to=2026-02-28"      # календарь: 29 выпусков
curl -s http://localhost:8080/api/forecasts/2026-02-13                         # выпуск: 144 строки, weather_runs
curl -s -o 2026-02-13.csv http://localhost:8080/api/forecasts/2026-02-13.csv   # CSV выпуска
curl -s -o feb.csv http://localhost:8080/api/forecasts/february.csv            # сводный CSV за февраль
curl -s http://localhost:8080/api/traces/2026-02-13                            # записанная лента шагов
curl -s http://localhost:8080/api/metrics                                      # январь: модель и базовые линии
curl -s "http://localhost:8080/api/live/journal?limit=20"   # что агент сделал сам — TODO(16:15): проверить

# запустить агента и смотреть его шаги вживую (SSE); без ключа — детерминированно
ID=$(curl -s -X POST http://localhost:8080/api/runs -H 'content-type: application/json' \
  -d '{"issue_date": "2026-02-13", "trigger": "new_weather_run"}' \
  | python3 -c 'import json, sys; print(json.load(sys.stdin)["id"])')
curl -N http://localhost:8080/api/runs/$ID/events

# сбой источника погоды: агент повторяет запрос и предупреждает (TODO(16:15): проверить, что API передаёт scenario)
curl -s -X POST http://localhost:8080/api/runs -H 'content-type: application/json' \
  -d '{"issue_date": "2026-02-13", "scenario": "weather_outage"}'

# неверный запрос — 400 с текстом, а не 500
curl -s http://localhost:8080/api/forecasts/2026-13-45
# {"error":"Неверная дата выпуска «2026-13-45»: нужен формат ГГГГ-ММ-ДД"}
```

## 9. Результаты

**29 выпусков за февраль** — в [`outputs/`](outputs/), они же видны в интерфейсе:

| Файл | Что |
|---|---|
| `outputs/forecasts/{D}.csv` | выпуск D = 2026-01-31 … 2026-02-28, последняя версия, 144 строки |
| `outputs/forecasts/{D}.json` | запись выпуска со всеми версиями (v1, v2), прогонами погоды, флагами, сводкой — источник для API |
| `outputs/forecast_feb2026.csv` | все 29 выпусков одним файлом, 29 × 144 = 4176 строк |
| `outputs/traces/{D}.jsonl` | лента шагов агента для каждого выпуска — доказательство, что выпуск сделал агент |
| `outputs/metrics_jan.json` | метрики и ряды января для вкладки «Качество» |

**Формат CSV** (UTF-8, запятая, порядок столбцов фиксирован):

| Столбец | Пример | Смысл |
|---|---|---|
| `issue_date` | `2026-02-13` | дата выпуска D |
| `issue_time_local` | `2026-02-14T00:00+05:00` | момент выпуска T |
| `target_time_local` | `2026-02-14T00:00+05:00` | начало целевого часа; h = 1 → (D+1) 00:00 |
| `horizon_h` | `1` … `48` | горизонт, часов |
| `turbine` | `1`, `2`, `plant` | турбина или ВЭС (среднее двух) |
| `p10`, `p50`, `p90` | `0.2134` | мощность в долях номинала, [0, 1], 4 знака |
| `wind_fc_ms` | `7.41` | прогноз ветра, м/с (`TODO(16:15)`: высота — 100 м?) |
| `weather_init_max_utc` | `2026-02-13T06:00Z` | верхняя граница старта прогона погоды ([§ 4](#4-правило-без-будущего)) |
| `version` | `1` или `2` | версия выпуска: 2 — пересчитан на более свежем прогоне |

Пересчёт (v2) был в `TODO(16:15)` выпусках из 29: `TODO(16:15)` (даты).

**Точность на январе 2026.** Факта за февраль в выданных данных нет: они заканчиваются 31.01.2026 23:50.
Поэтому точность показываем на бэктесте января, который прошёл тем же путём: 30 выпусков
D = 31.12.2025 … 29.01.2026, модель обучена на данных до 01.01.2026. Февральские CSV готовы для сверки
организатором.

| Метод (ВЭС, все 48 ч) | nMAE, % номинала | nRMSE, % номинала |
|---|---|---|
| **Windcast, P50** | `TODO(16:15)` | `TODO(16:15)` |
| Кривая мощности по прогнозному ветру | `TODO(16:15)` | `TODO(16:15)` |
| Климатология: среднее по часу × месяцу | `TODO(16:15)` | `TODO(16:15)` |
| Персистентность: «завтра как сегодня» | `TODO(16:15)` | `TODO(16:15)` |

- Выигрыш против лучшей базовой линии: `TODO(16:15)` %.
- Покрытие P10–P90 (доля часов, где факт внутри коридора): `TODO(16:15)` % при цели ≈ 80 %.
- Ошибка по горизонту h = 1…48 — вкладка «Качество» и `GET /api/metrics` (`by_horizon`).
- `TODO(16:15)`: уточнить, по ВЭС или по всем турбинам считается строка таблицы.

## 10. Структура репозитория

```
backend/                 FastAPI и пакет windcast; Dockerfile собирается из корня репозитория
  windcast/
    paths.py timeline.py             общие: пути от WINDCAST_ROOT, момент выпуска T, горизонт, лаги
    config.py data.py                SCADA → почасовой ряд, check_data (Куба)
    weather.py model.py evaluate.py  погода Open-Meteo, модель, метрики января (Куба)   TODO(16:15)
    agent.py tools.py store.py       агент, 6 инструментов, запись выпусков (Абылай)
    ports.py _stubs.py               подключение функций Кубы; заглушки только до их появления
    api.py runs.py backtest.py       REST + SSE, прогоны в фоне, CLI выпусков подряд (Абылай)
  tests/                 pytest
frontend/                экраны (Сула); fixtures/ — синтетические ответы API для разработки интерфейса
data/raw/                CSV SCADA как выданы организатором, не изменяются
data/processed/          hourly.parquet — генерируется, не в git
data/weather_cache/      ответы Open-Meteo — запасной путь без сети
models/                  обученные модели
outputs/                 29 выпусков, ленты шагов, метрики января — результат для сдачи
scripts/verify.py        проверка выпусков для жюри (только стандартная библиотека)
infra/                   Caddy, compose для сервера, деплой push-to-deploy
docs/                    контракт, ТЗ, разбор данных, схема архитектуры, макет интерфейса
docker-compose.yml       запуск из чистого клона
```

В каждом каталоге есть свой `README.md`: что в нём лежит и кто владелец.

## 11. Сторонние компоненты и заготовки

Раскрытие по п. 5.4.4–5.4.4.2 положения.

**Открытые библиотеки и образы:**

| Компонент | Версия | Лицензия | Для чего |
|---|---|---|---|
| pandas | 2.2.3 | BSD-3-Clause | таблицы и временные ряды |
| NumPy | 2.2.2 | BSD-3-Clause | вычисления |
| PyArrow | 19.0.1 | Apache-2.0 | Parquet |
| LightGBM | 4.6.0 | MIT | модель P10/P50/P90 (`TODO(16:15)`: проверить) |
| scikit-learn | 1.6.1 | BSD-3-Clause | модель и метрики (`TODO(16:15)`: уточнить роль) |
| SciPy | 1.15.2 | BSD-3-Clause | зависимость LightGBM и scikit-learn |
| joblib | 1.4.2 | BSD-3-Clause | сохранение моделей |
| FastAPI | 0.115.12 | MIT | API |
| Starlette, Pydantic | транзитивно через FastAPI | BSD-3-Clause, MIT | HTTP, SSE, валидация |
| Uvicorn | 0.34.0 | BSD-3-Clause | ASGI-сервер |
| openai-python | 1.109.1 | Apache-2.0 | клиент OpenAI API |
| requests | 2.32.3 | Apache-2.0 | HTTP-клиент |
| HTTPX | 0.28.1 | BSD-3-Clause | тестовый клиент API |
| pytest | 8.3.5 | MIT | тесты |
| Ruff | 0.16.8 | MIT | линтер |
| build, setuptools | 1.2.2.post1, 75.8.0 | MIT | сборка пакета |
| Python (`python:3.12-slim`) | 3.12 | PSF-2.0 | среда бэкенда |
| Caddy (`caddy:2.11.4-alpine`) | 2.11.4 | Apache-2.0 | обратный прокси |
| Интерфейс | `TODO(16:15)` | `TODO(16:15)` | фреймворк, графики |

Лицензии Python-пакетов сверены с метаданными установленных пакетов.

**Данные:**

| Данные | Источник | Условия |
|---|---|---|
| SCADA двух турбин, 10 мин, 11.03.2023–31.01.2026 (`data/raw/`) | организатор HackAlem AI | выданы для задачи, в репозитории без изменений |
| Архивные и текущие прогнозы погоды | [Open-Meteo.com](https://open-meteo.com/) | [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/), без ключа, бесплатно для некоммерческого использования. **Weather data by [Open-Meteo.com](https://open-meteo.com/)** |
| Модель погоды ECMWF IFS 0,25° (через Open-Meteo) | ECMWF open data | CC BY 4.0 |

**Сервисы:** OpenAI API — выбор шагов агента, решения о пересчёте и тексты сводок, модель по умолчанию
`gpt-5-mini`. Числа прогноза LLM не создаёт.

**AI-ассистенты при разработке** (разрешено п. 5.4.12): Claude Code (Anthropic) и OpenAI Codex. С ними
писали код, тесты, документацию и ревью изменений. Каждое изменение закоммичено участником команды,
история — в git.

**Заготовки, подготовленные до хакатона** (п. 5.4.4.2). Это наш собственный инфраструктурный и процессный
набор. Готового продукта или основной части решения в нём нет:

| Что | Где | Когда |
|---|---|---|
| Инфраструктура деплоя: Caddy-конфиги, compose для сервера, push-to-deploy (bare-репозиторий с хуком), скрипт настройки сервера, дымовая заглушка для проверки деплоя | `infra/`, `templates/smoke/` | 22.09 |
| Процессные скрипты: чекпоинт с поиском секретов, ревью веток, наблюдатель за пушами | `scripts/checkpoint.sh`, `scripts/review.sh`, `scripts/review-prompt.md`, `scripts/watch.sh` | 22.09 |
| Шаблоны: README, контракт, слайды, сценарий видео, чек-лист сдачи | `templates/` | 22.09 |
| Справочники: образец цикла агента на OpenAI tool calling (~80 строк), схема событий следа, панель следа на React, памятка по данным | `docs/references/` | 22.09 |
| Правила работы команды и AI-ассистентов, шаблоны доски и рубрики, копии положения и критериев | `AGENTS.md`, `BOARD.md`, `docs/RUBRIC.md`, `docs/SCORE.md`, `docs/hackathon/` | 22.09; наполнены под ТЗ 23.09 |
| Workflow доставки задач через worktree и pull request (Куба) | `scripts/taskflow/`, `.agents/`, `.codex/`, `RTK.md`, `docs/task-workflow.md`, `docs/change-request-template.md` | подготовлен заранее, добавлен в репозиторий 23.09 в 14:20 (PR #1) |

`TODO(16:15)`: указать, вошёл ли код из `docs/references/agent_loop.py` в `agent.py`.

**Вся продуктовая функциональность написана в соревновательной части, 23.09 с 13:00 до 18:00.** Это
конвейер данных, клиент погоды, модель, агент и инструменты, API, интерфейс, `scripts/verify.py`, корневой
`docker-compose.yml`, `backend/Dockerfile`, контракт, схема архитектуры и макет. Первый коммит в
`backend/` и `frontend/` сделан 23.09 в 14:42. Проверить можно так:

```bash
git log --reverse --date=format:'%Y-%m-%d %H:%M' --format='%ad %an %s' -- backend frontend scripts/verify.py | head
```

## 12. Ограничения и потенциал развития

**Ограничения — честно:**

- **Одна погодная модель** (ECMWF IFS через Open-Meteo). Ансамбля нет, и коридор P10–P90 отражает только
  ошибку, выученную на истории. Флаг «расхождение погодных моделей» появляется, только если в погоде есть
  вторая модель. `TODO(16:15)`: есть ли она в `weather.py`.
- **Задержка публикации прогона 8 ч — консервативная константа.** Архив не хранит реальный момент выхода
  каждого прогона. Если прогон на деле выходил раньше, мы теряем немного свежести, но утечки нет.
- **Факта за февраль нет**, поэтому точность февраля не посчитана. Качество показано на январе, февральские
  CSV готовы для сверки организатором.
- **Прогнозируется выработка исправной турбины.** Простои и ограничения (≈ 1 % записей) погодой не
  предсказываются; в обучении они исключены через `valid = false`.
- **Архив Previous Runs есть примерно с середины 2024 года**, поэтому модель учится примерно на полутора
  годах. SCADA с 03.2023 до середины 2024 года в обучение модели не входит.
- **Ветер > 20 м/с в истории почти не встречался**, так что поведение на отсечке модель знает плохо.
  Такие часы агент помечает флагом `wind_gt20`.
- **Обледенение — эвристика по температуре** (от −3 до +1 °C), без влажности и осадков.
- **Прогоны агента хранятся в памяти процесса**, поэтому API работает с одним воркером uvicorn. Перезапуск
  обрывает незавершённые потоки SSE, а опубликованные выпуски остаются в `outputs/`. Live-выпуски и журнал
  лежат в `outputs/live/` внутри контейнера и не переживают его пересборку.
- **Open-Meteo бесплатен только для некоммерческого использования** и с лимитом запросов. Для эксплуатации
  нужен коммерческий тариф или прямая загрузка ECMWF open data.
- **Заглушки.** Пока функций погоды и модели нет, `ports.py` подставляет синтетические заглушки
  (`source = stub`, видно в ленте и в `/health` → `model_version`). `verify.py` падает, если заглушка
  есть хоть в одном выпуске. `TODO(16:15)`: подтвердить, что в итоговых выпусках заглушек нет.

**Что уже сделано нестандартно:**

- «Без будущего» встроено в систему, а не только соблюдено: правило в коде (`timeline.py`, отказ
  `recalc_forecast`), независимая проверка (`verify.py`) и «✓ Без будущего» на экране у каждого выпуска.
- Пересчёт запускается событием «вышел новый прогон», а решение принимает агент и объясняет его в ленте.
- Агент работает сам: Live-наблюдатель и журнал решений без участия человека.
- Прогноз вероятностный (P10–P90), а не одна цифра, с флагами рисков на оси времени.
- Детерминированный режим даёт те же числа без ключа, так что проверка не требует внешних аккаунтов.

**Куда развивать:**

- **«Спросить агента»**: вопросы диспетчера на естественном языке («почему в 15:00 спад?», «что будет,
  если ветер усилится?») с ответами через те же инструменты. Пока не реализовано.
- **Ансамбль погодных моделей** (ECMWF IFS, GFS, ICON — все есть в Open-Meteo, в том числе в Previous Runs):
  точнее P50, честнее коридор, флаг расхождения моделей на реальных данных.
- **Модель обледенения**: температура, влажность и осадки, поправка к P50/P10 в часы риска.
- **Несколько ВЭС**: новая станция — это координаты и история SCADA. Инструменты и правило «без будущего»
  от станции не зависят.
- **Экспорт графика** в формат системного оператора и уведомления диспетчеру о рампах.
- **MCP-адаптер над тем же реестром инструментов**: агента можно подключить к любому MCP-клиенту
  (например, к ассистенту диспетчера) без переписывания инструментов.

## 13. Команда

**Pushin' Code**

- **Абылай** — мастер: архитектура, агент и API, инфраструктура.
- **Куба** — данные, погода, модель.
- **Сула** — интерфейс.
