# <Название проекта>

<Одна фраза: что делает и какую проблему решает. Для кого.>

**Трек:** <трек> · **Кейс:** <кейс> · **Живая ссылка:** https://… · **Демо-видео:** <ссылка> · **Презентация:** <ссылка>

> TL;DR (EN): <one sentence for a non-Russian reader>

---

> Разделы ниже соответствуют критериям техотбора (положение, п. 7.4, редакция 17.09.2026).
> README оценивается в 20 баллов и **заменяет презентацию**: п. 7.4 — «по нему можно понять,
> что реализовано, как устроено решение, какие технологии и данные используются, как установить
> и запустить проект, как проверить основной сценарий и какие ограничения известны команде».

## Что реализовано

**Сквозной сценарий:** <кто → вводит что → агент делает что → какой результат с последствием>

Обязательные требования кейса:

| # | Требование кейса | Статус | Где в проекте |
|---|---|---|---|
| 1 | <дословно из кейса> | ✅ | `backend/…`, экран «…» |
| 2 | <…> | ✅ | |
| 3 | <…> | ⚠️ частично: <что именно не закрыто> | |

Скриншоты: `docs/screenshots/` (2–3 вставить сюда).

## Как устроено решение

<Схема: фронт → API → агент → инструменты → данные. ASCII или один абзац.>

```
<UI> ──POST /api/runs──> <API> ──> <агент: цикл выбора инструментов>
                                      ├── tool_1: …
                                      ├── tool_2: …
                                      └── tool_3: create_… (действие)
                            └──SSE /api/runs/{id}/events──> панель следа
```

- **Агент:** OpenAI <модель>, цикл с инструментами `…`, `…`, `create_…`. Модель выбирает
  инструмент сама; последний вызов — действие с последствием (создаёт / блокирует / назначает).
- **Бэкенд:** <стек, версия>
- **Фронтенд:** <стек, версия>, панель следа решения по SSE
- **Данные:** <источник>. Демо-данные создаются скриптом `<seed>` — в репозитории, не «у нас на машине».

## Технологии и данные

| Слой | Технология | Версия |
|---|---|---|
| Модель | OpenAI <…> | |
| Бэкенд | | |
| Фронтенд | | |
| Хранилище | | |
| Развёртывание | Docker Compose, Caddy | |

Данные: <синтетические, генерируются `<seed>` / выданный организатором датасет / публичный корпус
с указанием источника>. <Если корпус НПА или документов — перечислить с ссылками.>

## Установка и запуск

**Системные требования:** Docker 24+ с compose plugin, 2 ГБ свободной RAM, порты 80/443 (или 3000/8000 локально).
**Переменные окружения:** все перечислены в `.env.example`; обязательна только `OPENAI_API_KEY`.

```bash
git clone <url> && cd <repo>
cp .env.example .env          # вписать OPENAI_API_KEY
docker compose -f infra/docker-compose.yml up -d --build
<команда заполнения демо-данными>
```

Готовность: `curl http://localhost:8000/health` → `{"ok": true}`. Фронт: http://localhost:3000

## Как проверить основной сценарий

1. Открыть <адрес>, ввести: `<конкретный текст примера — тот, что точно отрабатывает>`
2. Нажать «<кнопка>». В панели справа появляется след: `<событие 1>` → `<событие 2>` → `<действие>`.
3. Ожидаемый результат: <что именно должно появиться на экране — дословно, чтобы проверяющий сверил>.
4. **Проверка нештатного случая:** ввести `<пример, который должен быть заблокирован>` →
   ожидается `<вердикт / блокировка>` с обоснованием `<…>`.

Быстрая проверка из консоли:

```bash
curl -s -X POST localhost:8000/api/runs -H 'content-type: application/json' \
  -d '{"text":"<пример>"}' | jq
```

## Известные ограничения

- <что не реализовано и почему — прямо. Лучше назвать самому, чем дать найти>
- <где используются синтетические данные вместо реальных>
- <нагрузка, на которую не рассчитывали; языки; форматы>
- Проект собран за 5 часов соревновательной части; production-готовность не заявляется.

## Надёжность и обработка ошибок

- Валидация входа: <что проверяется>. Некорректный запрос → `400` с текстом причины, не `500`.
- <Что делает система, если модель недоступна / превышен таймаут.>
- Секреты только в `.env` (в `.gitignore`), в коде и истории коммитов ключей нет.

## Раскрытие (положение, п. 6.4)

- **Подготовлено до хакатона:** конфигурация агентов (`AGENTS.md`), шаблоны документов,
  docker-compose и Caddyfile, вспомогательные скрипты. Продуктовый код написан во время хакатона.
- **Инструменты разработки:** Claude Code, OpenAI Codex.
- **Модели:** OpenAI <модели>.
- **Библиотеки:** <основные, с лицензиями>.
- **Данные:** <синтетические / выданные организатором / публичный источник со ссылкой>.

## Потенциал развития

<Для Demo Day: на техотборе баллов не даёт, но 20 баллов на Demo Day и раздел стоит дёшево.>

- <следующий шаг 1 — конкретный>
- <куда масштабируется: другие объекты / организации / отрасли>
- <что нужно для пилота: данные, доступ, срок>

## Команда

<Имя> — <роль> · <Имя> — <роль> · <Имя> — <роль>
